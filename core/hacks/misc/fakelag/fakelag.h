#pragma once
#include "../../../../dependencies/common_includes.hpp"
#include <random>

class c_fakelag {
	// sub calls
private:

	// main calls
public:
	void fake_lag( player_t *local );
}; extern c_fakelag *g_fakelag;